完整的一个方案指导。

可以。你现在这个问题其实已经从「做一个 cofactor benchmark」进入了更有研究价值的一层：

> **当纯 LLM 只看蛋白序列无法可靠判断辅因子时，Agent 应该主动调用哪些生物信息学工具来获取证据？**
> 

这甚至可以成为 benchmark 最核心的实验变量。

我建议先把 Agent 的工具分成 **5 类证据源**：

| Tool 类型 | Agent 得到什么 | 为什么能提高命中率 |
| --- | --- | --- |
| **Sequence similarity** | 找相似蛋白/家族 | 同源蛋白往往共享辅因子 |
| **Domain / motif** | 功能域、保守 motif | 很多辅因子存在特征性结合模式 |
| **Structure prediction** | 3D 结构 | 判断 binding pocket / 配位几何 |
| **Structure similarity** | 找结构相似蛋白 | 低序列同源时仍可能找到功能关系 |
| **Literature / database search** | 外部生物学证据 | 用机制、家族、实验文献辅助判断 |

### 1. 最重要：sequence similarity

比如给 Agent 一个未知序列：

```
MAVK...
```

它先自己判断不确定，于是调用类似：

```
search_similar_sequence(sequence)
```

底层可以是 MMseqs2 / BLAST / DIAMOND。

返回：

```
Hit 1
identity: 42%
coverage: 91%
annotation: aminotransferase
cofactor: PLP

Hit 2
identity: 38%
coverage: 87%
annotation: aminotransferase
cofactor: PLP
```

Agent 就获得很强的 PLP 证据。

这会形成你最重要的一条 baseline：

**LLM memory-only → LLM + homology retrieval**

而且我们还能专门测试：

> 当 identity 从 70% → 40% → 20% 下降，Agentic retrieval 的收益怎么变化？
> 

这就非常像一个正规的科学问题。

---

### 2. Domain / motif tool

例如 Agent 调：

```
scan_domains(sequence)
```

底层可以接 InterProScan、HMMER/Pfam、PROSITE。

返回：

```
Domain:
aminotransferase class I/II

Conserved residues:
NNRYGGHGS...
```

Agent 再推理：

> aminotransferase family + conserved PLP-binding architecture → PLP probability high
> 

这和 sequence search 是不同的信息来源：

**Sequence search：**

“它像哪个已知蛋白？”

**Domain/motif：**

“它具有哪种功能模块？”

对于低同源蛋白，这个区别非常重要。

---

### 3. Structure tool

Agent 可以调用结构预测，例如：

```
predict_structure(sequence)
```

得到结构后，再做 pocket / metal-site / ligand-binding analysis。

例如 Zn²⁺ 酶可能出现特定的 His/Cys/Asp/Glu 配位环境；Fe-S 蛋白可能存在特征性的 Cys 空间排列。

这里 Agent 不应该只是：

> “看到几个 Cys → 猜 [4Fe-4S]。”
> 

而是结合：

```
sequence
+
domain
+
3D geometry
+
known chemistry
```

做 mechanistic reasoning。

这会是非常有价值的 **Structure-Augmented Agent** baseline。

---

### 4. Structure similarity search

这个我认为尤其值得你后面做。

比如一个蛋白：

```
sequence identity < 20%
```

MMseqs2 基本找不到靠谱同源关系。

但预测结构后用 Foldseek：

```
search_structure(predicted_structure)
```

可能找到：

```
Known enzyme X
TM-score: 0.81
cofactor: FAD
function: oxidoreductase
```

于是出现一个很漂亮的问题：

> **当 sequence homology retrieval 失效以后，structure retrieval 能不能 rescue cofactor annotation？**
> 

可以设计：

```
Direct LLM
      ↓
Sequence Retrieval
      ↓
Domain Retrieval
      ↓
Structure Retrieval
      ↓
Full Agent
```

然后观察性能增益。

---

### 5. Literature / biological database search

最后才是更开放的 Agent search。

例如 Agent 已经通过 domain 判断：

```
protein belongs to radical SAM family
```

然后主动搜索：

> Radical SAM enzymes commonly require what Fe-S cluster?
> 

再结合序列 motif：

```
CxxxCxxC
```

最后判断 `[4Fe-4S]`。

这里 Agent 真正在做的是：

**观察 → 提假设 → 搜证据 → 验证 → 输出 annotation。**

---

## 但这里有一个 benchmark 的致命点：不能让 Tool 直接把答案查出来

比如 benchmark 样本来自 UniProt：

```
UniProt ID = P12345
Gold = FAD
```

如果 Agent 可以直接：

```
query_uniprot(P12345)
```

然后 UniProt：

```
Cofactor: FAD
```

那 Accuracy 99% 也没有意义。

所以必须设计 **information firewall**：

```
                 Benchmark sequence
                        │
                        ▼
                     Agent
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
 Sequence search    Domain search   Structure search
        │               │               │
        ▼               ▼               ▼
 Reference DB       Pfam/InterPro    Structure DB
        │
        X
  Test protein annotation
```

也就是说：

> **Tool 可以提供证据，但不能直接访问测试样本的 gold annotation。**
> 

这可能是整个 benchmark 最关键的 methodological contribution 之一。

---

## 这样你的实验就非常清晰了

最终不是简单比较：

```
LLM A vs LLM B
```

而是做一个 **evidence ablation ladder**：

| Setting | LLM | Homology | Domain | Structure | Search |
| --- | --- | --- | --- | --- | --- |
| Direct | ✓ |  |  |  |  |
| + Homology | ✓ | ✓ |  |  |  |
| + Domain | ✓ | ✓ | ✓ |  |  |
| + Structure | ✓ | ✓ | ✓ | ✓ |  |
| Full Agent | ✓ | ✓ | ✓ | ✓ | ✓ |

然后最值得研究的其实不是 Full Agent 最终从 **55% → 82%**。

而是：

> **不同证据源在什么情况下真正帮助了模型？**
> 

比如最后可能得到：

```
Easy homology
Direct       72%
+ Sequence   94%
+ Structure  95%

Hard homology
Direct       38%
+ Sequence   45%
+ Domain     57%
+ Structure  69%
Full Agent   74%
```

这时候你的论文故事就出来了：

> **同源信息丰富时 sequence retrieval 足够；进入 remote-homology regime 后，domain 和 structural evidence 才开始产生主要增益。**
> 

这比单纯说「Agent 比 LLM 准 20%」有科学意义得多。

所以我认为你这个 benchmark 后面真正值得研究的方向不是 **Cofactor 4-class → 104-class** 本身，而是：

> **Cofactor annotation 作为一个 controlled testbed，研究 LLM 到底如何利用 sequence、homology、domain、structure、literature 等不同层次的 biological evidence。**
> 

这已经开始接近一个 **Enzyme Annotation Agent Benchmark** 的雏形了。

这里的 **「长尾问题」** 其实是 4 类扩到 104 类以后最核心的问题之一。

你先记住一句话：

> **长尾 = 少数类别有大量样本，大量类别只有极少样本。**
> 

### 放到我们的辅因子数据里

假设严格数据池里有 104 种 cofactor，但它不是：

```
104 类 × 每类约 50 条
```

而更像：

```
Mg²⁺        ████████████████████  1851
Mn²⁺        █████████             900
Zn²⁺        ███████               731
FAD         █████
PLP         ████
...
稀有辅因子  █                     8
稀有辅因子  ▏                     3
稀有辅因子  ▏                     1
```

我们之前解析出来的结果里，**104 类中约 70 类不到 10 条**。

这就是典型的 **long-tail distribution**：

**头部 head classes**：Mg²⁺、Mn²⁺、Zn²⁺ 等，数据非常多。

**尾部 tail classes**：很多冷门 cofactor 只有 1、2、5 条。

---

## 为什么这对 benchmark 很麻烦？

最直观的问题是 **Accuracy 会骗人**。

假设：

```
Mg²⁺  1800 条
Rare-A   3 条
Rare-B   2 条
Rare-C   1 条
...
```

一个模型如果特别会预测 Mg²⁺，但 70 个稀有 cofactor 几乎全部不会，它的总体 Accuracy 仍然可能不错。

于是你会得到：

> Model A Accuracy = 75%
> 

但真正的问题可能是：

> 它只认识常见辅因子，面对生化长尾基本失效。
> 

所以我们才一直强调 **Macro-F1**。

Macro-F1 本质上是：

```
F1(Mg²⁺)
F1(Mn²⁺)
F1(Zn²⁺)
...
F1(Rare cofactor 104)

↓ 每一类平等

求平均
```

这样 1800 条 Mg²⁺ 和 5 条稀有辅因子在“类别层面”权重一样。

---

## 但 Macro-F1 也救不了 1～5 条的类别

这是第二层问题。

如果某个 cofactor：

```
整个 UniProt 严格数据池
只有 3 条
```

你测：

```
Model A：3/3
Model B：2/3
```

看起来：

```
A = 100%
B = 67%
```

但这个差异几乎没有统计稳定性。

**所以不能因为有 104 个 ChEBI 标签，就宣称自己有一个稳定的 104-class classification benchmark。**

这也是我之前建议：

```
104 类全部保留
        │
        ├── Head/Core classes
        │      样本足够
        │      → 正式 Macro-F1 排名
        │
        └── Tail classes
               样本极少
               → 单独做 Long-tail challenge
```

的根本原因。

---

## 但有意思的地方恰恰在这里

如果你的研究对象是 **Agent**，长尾反而可能从“数据缺陷”变成一个很好的科学问题。

想一下：

### 常见辅因子

模型预训练期间可能已经大量见过：

```
PLP
FAD
Mg²⁺
Zn²⁺
```

所以：

> LLM memory 本身就可能很强。
> 

### 极稀有辅因子

模型可能根本记不住。

这时候：

```
Sequence only
     ↓
LLM
     ↓
不知道

但是

Sequence
  ↓
HMMER
  ↓
remote protein family
  ↓
MMseqs / Foldseek
  ↓
找到相关实验蛋白
  ↓
literature / mechanism evidence
  ↓
rare cofactor
```

Agent 才可能把它救回来。

所以你甚至可以把论文问题定义成：

> **Can agentic evidence acquisition overcome the long-tail problem in enzyme annotation?**
> 

这比单纯做：

> “LLM 能不能四选一预测 PLP/FAD/Zn/[4Fe-4S]？”
> 

研究问题强很多。

---

### 最终 benchmark 可以有三档

我反而建议以后不要只按照 sequence identity 分难度。

可以同时定义两个正交轴：

```
                 Label Frequency
              Head    Mid    Tail
            ┌──────┬──────┬──────┐
High homol. │ Easy │      │      │
            ├──────┼──────┼──────┤
Mid homol.  │      │      │      │
            ├──────┼──────┼──────┤
Low homol.  │      │      │ HARD │
            └──────┴──────┴──────┘
```

**轴 1：知识稀有度**

```
Head：≥100 samples
Mid：10–99
Tail：<10
```

**轴 2：序列/进化难度**

```
Easy：nearest homolog >50%
Medium：30–50%
Hard：<30%
```

这样会出现一个特别重要的测试区域：

> **Tail cofactor + Low homology**
> 

模型既不能靠预训练中的常见知识，又不能简单复制近邻答案。

这才是真正的 hard set。

---

而且你现在可以看到为什么我不建议今晚直接从 4 → 104。

不是因为 104 类做不了。

恰恰相反，**104 类可能才是最终更有研究价值的 benchmark。**

只是：

```
4-class
↓
验证 pipeline 是否成立

18-class Core
↓
验证大规模分类是否成立

104-class Long-tail
↓
研究真实生化长尾

104-class + Agent
↓
研究 Agent 能否主动获取证据
解决 rare + remote-homology annotation
```

这个递进关系一旦想清楚，你这个项目就不只是「做一个辅因子分类数据集」了，而是在用 **cofactor annotation 作为一个受控实验场，研究 LLM/Agent 面对蛋白质功能注释中的知识长尾、远缘同源和外部证据获取问题。**

历史对话：
今天就跑 llm only 就可以了，48条你要怎么选？







15:25






今天只跑 LLM-only，即模型端不给任何工具。但数据构建仍然必须使用 MMseqs2 计算同源难度，否则48条只是随机样本，无法解释为什么有的题难、有的题容易。
48条按下面方式选：
4个标签 × 3个同源难度 × 每格4条 = 48条
标签	Easy	Medium	Hard	合计
PLP	4	4	4	12
FAD	4	4	4	12
Zn²⁺	4	4	4	12
[4Fe-4S]	4	4	4	12


具体筛选流程：
1. 只保留标签级显式带 ECO:0000269 和 PMID 的记录。
2. 只保留恰好一个目标辅因子的记录。
3. 排除同时存在其他辅因子、替代辅因子或模糊表述的记录。
4. 排除 fragment、异常长度、非法氨基酸。
5. 完全相同序列只留一个；标签冲突则全部进入人工复核。
6. 其余记录作为 reference pool，用 MMseqs2 找每条候选的最佳非重复同源序列。
7. 按最佳命中划分：
   - Easy：50%–90% identity
   - Medium：30%–50%
   - Hard：低于30%或没有有效命中
   - alignment coverage 至少70%
8. 每个“标签×难度”格子中，不是直接取前4条，而是：
   - 优先实验依据最清楚的；
   - 不重复选择高度相似家族；
   - 尽量覆盖不同物种；
   - 对剩余同等级候选使用固定随机种子抽取。
9. 选完48条后，从reference pool删除测试序列及其超过90%的近重复，再重新计算一次难度；难度变化的样本被替换，直到48条稳定。
10. 最后把48条全部导出给你人工审核，通过后才跑LLM。
这样最终结果能回答：
在 PLP、FAD、Zn²⁺、[4Fe-4S] 四类上，LLM-only 的能力如何随序列同源信息难度变化？

我建议就锁定这个 4 × 3 × 4 选择方案。


15:27








7008 条里面只有 48 条对应这四类cofactor 吗？







15:27






不是。48条只是我们从大量合格候选中抽取的平衡测试集。
7,008条是所有辅因子类型的候选池。我们之前解析到，显式带 ECO:0000269 的四类记录大约有：
辅因子	候选记录数
Zn²⁺	731
FAD	514
PLP	373
[4Fe-4S]	230


合计约1,848个标签命中，但其中存在：
- 同一酶有多个辅因子；
- 目标辅因子之外还有其他辅因子；
- 重复序列；
- 模糊或替代性描述。
所以最终严格单标签候选会少于1,848条，准确数字需要运行清洗脚本后才能确定。
关系是：
7,008条全部辅因子候选
  ↓ 标签级ECO:0000269
5,337条严格实验候选
  ↓ 只取目标四类
约1,848个边际命中
  ↓ 单标签、无歧义、去重复
四类严格候选池（待实际清洗得到）
  ↓ 平衡抽样
48条正式测试集
选择48条不是因为只有48条，而是为了今天用较低调用成本跑通：
4类 × 3种难度 × 每格4条 = 48
剩余合格记录不会丢弃：它们用于同源难度计算、未来扩大测试集，以及后续Agent的冻结reference library。


15:27








你是怎么统计个数的？？







15:53






这些数字不是刚才那份代码跑出来的。你要求审核前不执行，所以下载器还没有运行。它们来自我之前做的一次临时只读统计，目前只能叫探索性数字，不能作为最终论文统计。
具体过程是：
1. 先查询7,008条候选
使用 UniProt REST 查询：
reviewed:true
AND ec:*
AND fragment:false
AND length:[50 TO 1100]
AND cc_cofactor_chebi_exp:*
查看对应UniProt查询
API当时返回 X-Total-Results: 7008。
2. 分页读取全部记录
每页500条，共15页，读取 accession 和完整 Cofactor comment。
3. 逐个解析辅因子标签
对每条记录提取：
Name
ChEBI ID
Evidence code
PubMed ID
只在具体辅因子标签的 evidence 中出现：
ECO:0000269
时才计数。
逻辑等价于：
for protein in records:
    for cofactor in protein.cofactors:
        if "ECO:0000269" in cofactor.evidence_codes:
            counts[cofactor.chebi_id].add(protein.accession)
这里使用集合保存 accession，避免同一条记录重复计数。
4. 得到标签级统计
ChEBI	标签	独立accession数
CHEBI:29105	Zn²⁺	731
CHEBI:57692	FAD	514
CHEBI:597326	PLP	373
CHEBI:49883	[4Fe-4S]	230


注意：这些是边际计数。一个accession可能同时出现在两个标签中，所以不能直接把1,848当作独立蛋白数量。
5. 为什么7,008最后只有5,337条显式证据？
因为 cc_cofactor_chebi_exp:* 是数据库搜索候选条件，不应直接等同于：
每一个返回记录的每一个辅因子标签都显式带 ECO:0000269。

实际解析时：
- 5,337条至少有一个具体标签显式带 ECO:0000269；
- 其余1,671条没有通过我们更严格的标签级解析。
这正说明不能只相信UniProt查询返回数量。
当前数字的局限
此前统计使用的是 Cofactor 文本字段解析，不是我们最终要采用的完整结构化JSON清洗器，因此：
- evidence可能出现在note等不同层级；
- 多辅因子语义还未判断；
- 替代关系还未清理；
- 重复序列还未去除。
所以 731、514、373、230是候选统计，不是最终金标准数量。
正式流程应该改成解析UniProt结构化JSON对象，并输出“每条记录为什么被计入”的审计表。等你审核下载器后，我们先运行原始下载；下一份代码只负责结构化解析和计数，你审核通过后再执行。


15:54









